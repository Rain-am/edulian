# 领星接口文档

请把领星官方接口文档导出文件、关键接口截图或字段说明放在本目录。

实现真实 API 对接时，重点确认：

- 授权/令牌接口的请求方式、签名规则、Header 名称。
- FBA发货单列表、详情、箱明细接口。
- SKU/本地产品详情接口中的报关名、单位、重量、尺寸字段。
- 采购单、供应商、采购方接口。
- 库存批次或采购来源追溯接口。

当前真实 API endpoint 集中配置在 `src/fetchers/lingxing_api.py`，确认文档后只需要替换 `TODO/...` endpoint 和对应字段映射。

